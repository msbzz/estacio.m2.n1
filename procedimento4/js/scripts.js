function verificarTamanhoPagina() {
    // Obtenha o comprimento atual da página
    var comprimentoAtual = window.innerWidth;
    var alturaAtual = window.innerHeight;
    alert("A largura da sua página é de " + comprimentoAtual + " e altura de " + alturaAtual + " pixels!");
  }
  
  verificarTamanhoPagina()
  